import React from 'react'

const PathName = ({ pathName }: { pathName: string }) => {
    return (
        <h1 className='text-3xl font-semibold text-[#4D4D4D]'>{pathName}</h1>
    )
}

export default PathName